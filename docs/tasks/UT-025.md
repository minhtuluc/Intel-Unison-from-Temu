# UT-025 — ADR P2P, đặc tả signaling và giao thức đo traffic

Status: done (M5 — phần thiết kế; data plane chưa thi công)
Depends on: —
Owner / file ownership: `docs/adr/0005-p2p-transport-authority.md`, `docs/protocol/p2p-signaling.md`, `docs/benchmarks/p2p-traffic-protocol.md`, `CONTEXT.md`

Problem and evidence: chưa có tài liệu nào quy định điều gì phải giữ nguyên khi byte không còn đi qua host; `CONTEXT.md` chỉ ghi "P2P … chưa triển khai". Mốc M5 yêu cầu "đo traffic chứng minh payload A↔B không đi qua host" nhưng chưa định nghĩa phép đo đó, nên mọi tuyên bố P2P sẽ không kiểm chứng được.

In scope: ADR-0005 (luật chọn transport, host chỉ signaling, ràng buộc chuyển tiếp có kiểm soát, framing/backpressure/integrity, fallback tự động, giới hạn không STUN/TURN, phần không thuộc phạm vi); đặc tả event signaling + mã lỗi + giới hạn; giao thức đo định nghĩa trước máy/mạng/file/số lần/chỉ số; cập nhật từ vựng và cam kết trong `CONTEXT.md`.

Out of scope: mọi thay đổi code (thuộc UT-026/027/028).

Acceptance criteria: ADR không mâu thuẫn ADR-0001…0004 (đặc biệt: consent trước, quyền quyết định thuộc receiver, state trong runtime); "không đi qua host" được định nghĩa bằng quan sát server-side chứ không bằng cảm nhận tốc độ; tài liệu nói rõ P2P chưa hoạt động cho tới khi gate UT-026 xong.

Regression scenario: `npm run format:check` bao gồm `docs/` và `CONTEXT.md` nên lệch format sẽ fail gate.

Verification commands and actual results: `npm run quality` — **516/516 pass, exit 0**, coverage 95,49% line / 87,08% branch / 94,00% function.

Remaining risks and handoff: đây là thiết kế, **chưa có dòng code nào** cho P2P; mọi khẳng định về khả thi kỹ thuật vẫn phụ thuộc UT-026.
