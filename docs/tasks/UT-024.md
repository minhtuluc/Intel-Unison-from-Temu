# UT-024 — Tài liệu và cấu hình cho M4

Status: in-progress (chờ QC)
Depends on: UT-020, UT-021, UT-022, UT-023
Owner / file ownership: `docs/adr/0004-relay-receiver-authority.md` (mới), `CONTEXT.md`, `REPORT-ROADMAP.md`, `README.md`, `docs/tasks/UT-02x.md`

Problem and evidence: `CONTEXT.md` xếp "Receiver" vào thiết kế tương lai ("hiện chỉ host nhận upload") và "Relay" không nói gì về quyền của người nhận; chưa có ADR nào quy định host **không** duyệt và **không** tải được file relay. `docs/agents/domain.md` yêu cầu không âm thầm đổi nghĩa host/receiver/relay.

In scope: ADR-0004 (mô hình quyền, khóa danh tính, ACL, TTL/quota, giới hạn, phần không thuộc phạm vi); cập nhật bảng thuật ngữ + cam kết của CONTEXT.md; thêm UT-020…UT-024 và dòng M4 vào REPORT-ROADMAP.md kèm bằng chứng và giới hạn; README mô tả gửi cho người nhận; task file cho từng ID.

Out of scope: mô tả M5/P2P; tuyên bố đã kiểm browser/thiết bị.

Acceptance criteria: mọi tài liệu ghi đúng trạng thái **chưa merge — chờ QC**; ADR-0004 không mâu thuẫn ADR-0001/0002/0003; không có chỗ nào mô tả relay như tính năng đã kiểm chứng trên thiết bị thật; danh sách ID trong roadmap khớp file `docs/tasks/`.

Regression scenario: `npm run format:check` và `npm run lint` bao gồm vùng tài liệu (`AGENTS.md`, `CONTEXT.md`, `REPORT-ROADMAP.md`, `docs/`) nên lệch format sẽ fail gate.

Verification commands and actual results: `npm run quality` — 510/510 pass, exit 0 (format/lint sạch trên toàn bộ vùng tài liệu).

Remaining risks and handoff: nội dung M4 trong roadmap phải được cập nhật lại **sau khi QC kết luận** (đổi `in-progress` → `done` chỉ khi acceptance đạt và QC duyệt).
