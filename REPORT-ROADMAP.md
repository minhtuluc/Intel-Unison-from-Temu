# UniversalTrans — báo cáo cải thiện và roadmap

Ngày: 2026-09-14. Phạm vi: repository local, Node/Express, HTTP upload/download, WS, browser UI, launcher và tests. Tài liệu này là backlog chuẩn cho agent; không phải cam kết các tính năng tương lai đã triển khai.

## 1. Trả lời về mô hình kết nối

Hiện tại là mô hình host trung tâm:

```text
Client A -- HTTP file / WS events --> HOST <-- HTTP file / WS events -- Client B
```

Host lưu file tạm và file nhận, phục vụ download. Chưa có WebRTC, signaling offer/answer/ICE hoặc kênh dữ liệu trực tiếp A↔B. Client có thể đăng file lên shared staging để người khác tải lại từ host; đó là relay, chưa có chọn receiver, quyền riêng tư hay approval theo người nhận.

Hai hướng phát triển cần phân biệt:

- Relay có receiver: host chuyển tiếp A→B, thực hiện nhanh hơn trên nền hiện tại nhưng tốn đĩa/băng thông host.
- P2P thật: host chỉ discovery/signaling, file qua WebRTC DataChannel A↔B; cần backpressure, checksum, flow control, consent, xử lý ICE failure và fallback rõ ràng.

## 2. Bản vá thực hiện cùng tài liệu

### UT-001 — chỉ host được duyệt file (P0)

Trước sửa: `src/websocket/handlers.js` gửi `upload:request` cho tất cả socket. `src/routes/transfer.js` cho mọi HTTP client đọc pending và accept/decline. Popup xuất hiện trên tất cả máy; người gửi cũng tự bấm Accept được.

Sau sửa:

- `src/middleware/host-auth.js` cấp random capability riêng cho mỗi app/process và kiểm tra kết nối từ chính máy host; kiểm tra Origin nếu có. Không tin `isHost`, OS, tên máy hay X-Forwarded-For.
- Launcher mở URL host riêng chứa khóa trong fragment. `public/js/host-session.js` chuyển khóa vào sessionStorage và xóa fragment khỏi thanh địa chỉ. QR/public URL không chứa khóa.
- Pending/decision yêu cầu X-Host-Token hợp lệ. Client không quyền nhận HTTP 403 trước thao tác đĩa.
- WS registration xác minh khóa tại server. Popup chỉ gửi cho socket host đã xác minh, không dựa trên device registry do client đặt tên.
- UI dùng danh sách chờ, đọc lại pending khi host đăng ký/reconnect và sau quyết định. Nhiều upload đến không thay popup đầu bằng file cuối.
- Service-worker cache chuyển v3 và thêm module host-session để đưa bản vá tới shell mới.
- URL launcher dùng port thực tế sau listen; lỗi listen được reject về caller. Cấu hình QR `/api/info` vẫn cần UT-005.

Kiểm thử: `tests/integration/host-authorization.test.js` dùng host + sender + observer WS và HTTP thực; giả mạo role/token không nhận popup, client accept/decline 403, host accept giữ đúng nội dung. Kiểm tra socket remote, foreign Origin và khóa mới khác khóa cũ ở authority. Suite approval hiện hữu kiểm tra host accept/decline cả simple và chunked.

Trạng thái: đã triển khai và kiểm thử Node trên Windows; browser/điện thoại thật và CI Linux chưa chạy trong phiên này. Đây chưa phải bản vá toàn bộ auth của ứng dụng.

Kết quả cuối sau vá: `npm run quality` pass trên Windows / Node 24.15.0; 143/143 test, lint và format pass; coverage mã được load: 83,49% line, 78,94% branch, 83,77% function. Có thêm unit test bootstrap host-session. Các kiểm tra này không thay thế browser E2E/thiết bị thật. `.gitattributes` cố định LF cho code/docs để checkout Windows không làm format gate lỗi vì CRLF.

### Cách vận hành sau vá

Chạy `npm start`: dùng cửa sổ browser do host launcher mở để duyệt. Điện thoại quét QR/public URL. Nếu không tự mở browser hoặc dùng noBrowser, terminal in dòng `Host approval URL (private; do not share)`; chỉ mở liên kết này trên chính máy host. Mở URL client trên host chưa đủ quyền. Restart server cần mở lại phiên host từ launcher vì khóa thay đổi. Không gửi URL riêng cho người khác hoặc chụp log chứa khóa.

Thư mục nhận mặc định trùng `Downloads/UniversalTrans`, cũng đang là vị trí checkout trên máy hiện tại: nên tách thư mục dữ liệu khỏi source khi đóng gói. Từ M3 thư mục nhận đổi được lúc chạy qua `PATCH /api/settings` (host-only), nên không cần restart — nhưng giá trị mặc định vẫn nên đổi trước khi đóng gói.

Lưu ý lịch sử: đoạn trên mô tả hành vi **trước** M3 (upload đầy đủ vào temp rồi mới hỏi host). Hành vi đó đã được thay bằng consent trước truyền ở UT-012; xem `docs/adr/0003-consent-before-transfer.md`.

### UT-002 / UT-003 / UT-005 / UT-010 / UT-015 — lô M1 (2026-09-14, sau UT-001)

Năm mục P0/P1 của M1 đã triển khai trong cùng lượt với UT-001, chạy trên Node 22.23.1 / Linux. Quyết định và hệ quả: `docs/adr/0002-session-runtime-authority.md`.

- UT-003: nhánh JSON `POST /api/share` yêu cầu host capability; `validatePath` chạy trước mọi truy cập đĩa khi có `allowedSourceDirs` (mặc định rỗng), kiểm tra cả realpath nên symlink trỏ ra ngoài bị từ chối; null-byte bị chặn kể cả với host. Kiểm thử: `tests/integration/share-path-authorization.test.js` (6 ca, gồm multipart vẫn mở cho client).
- UT-002: `src/middleware/session-auth.js` cấp token opaque có expiry/revoke; PIN bật thì mọi route dữ liệu và broadcast WS yêu cầu session hoặc host capability; cookie `utrans_session` (HttpOnly, SameSite=Strict) cho URL media; bỏ token deterministic cũ. Kiểm thử: `tests/unit/session-auth.test.js`, `tests/integration/session-authorization.test.js`, cổng PIN frontend `public/js/api.js` + `tests/unit/api-client.test.js`.
- UT-015: không còn `path`/`filePath` trong response; danh tính do server cấp theo `connectionId`, tên client khai chỉ là `label` với `labelUntrusted: true`. Kiểm thử: `tests/integration/no-internal-path-leak.test.js`, `tests/unit/discovery.test.js`.
- UT-005: `src/runtime.js` gom config/managers/discovery/host capability/session store; route và service đọc `app.locals.runtime`; bỏ singleton `config`; static phục vụ theo package; `/api/info` báo port listener thật (đúng cả port 0). Kiểm thử: `tests/integration/runtime-isolation.test.js`. Toàn bộ test cũ đã chuyển sang runtime riêng, không còn sửa config toàn cục hay ghi vào `temp/` của repo.
- UT-010: `runtime.stop({timeoutMs})` terminate WS rồi đóng HTTP trong deadline, không `process.exit`, không đăng ký signal; `bin/utrans.js` sở hữu SIGINT/SIGTERM; `PORT_IN_USE` báo rõ; file instance `os.tmpdir()/utrans-<port>.json` để script dừng đúng PID. Kiểm thử: `tests/integration/lifecycle.test.js`; smoke thật: SIGTERM khi có WS mở → thoát trong ~100ms, exit code 0, xoá file instance.

Phát hiện thêm khi kiểm chứng và đã sửa trong lượt này: `/api/shared` trả `304` theo ETag nên browser hiển thị danh sách file cũ/rỗng sau khi đổi trạng thái — nay mọi response `/api` là `no-store` và bỏ `If-None-Match` (`tests/integration/api-cache.test.js`).

Review độc lập trên diff M1 phát hiện 7 vấn đề trong chính bản vá này (1 bypass P0 qua content-type, 1 đường phóng đại RAM ở chunk upload, rò rỉ registry, config session sai kiểu, giới hạn multipart bỏ qua runtime, WS bỏ qua cookie phiên, script Windows có thể kill nhầm PID). Tất cả đã sửa kèm test hồi quy; chi tiết ở mục "Sửa đổi sau review" trong ADR-0002.

Bằng chứng gate cuối lượt M1: sau khi khắc phục các lỗi review vòng 1 và vòng 2 (R1–R7), `npm run quality` pass trên Node / Linux và Windows — 207/207 automated tests pass, lint và format sạch, coverage 93,68% line / 88,60% branch / 93,49% function. Kiểm chứng browser thủ công bằng headless Chrome (CDP): cổng PIN hiện khi bật PIN, sai PIN báo lỗi và không lưu token, đúng PIN mở được danh sách file đã stage, bốn view render không lỗi console. Đã thêm regression test suite tại seam HTTP/WS thật khóa chặt R1–R7, timeout/never-settling child process handling, và junction fallback cho Windows. Toàn bộ 4 jobs CI (Ubuntu/Windows × Node 22/Node 24) đã pass 100% và PR #1 đã được merge chính thức vào `main` (commit `64cff86`). Giới hạn còn lại: chưa test trên điện thoại thật, chưa có TLS.

## 3. Bằng chứng và giới hạn audit

Baseline trước vá: 139/139 test pass, ESLint pass, Node coverage 83.34% line; `public/js/transfer.js` chỉ 43.28%. Coverage không bao gồm đầy đủ UI không được load. Suite xanh vẫn bỏ sót authorization và lỗi orchestration.

Đã tái hiện trước vá: bật PIN nhưng stage/download tệp fixture không token vẫn thành công; port 0 quảng bá sai URL; task awaiting_approval chiếm slot và chặn task kế tiếp. Đã đọc trực tiếp code để xác định các vấn đề còn lại bên dưới; mục ghi “cần tái hiện” không được trình bày như kết quả benchmark hay race đã chứng minh.

## 4. Backlog lỗi cần vá

| ID     | Priority / status                          | Hiện trạng, nguồn                                                                                                                                                                                                                       | Acceptance criteria và kiểm thử cần có                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UT-002 | P0 / done (M1)                             | PIN chỉ tạo token, không được enforce ở share/download/upload/WS; `routes/info.js`, `server.js`.                                                                                                                                        | Session ngẫu nhiên có expiry/revoke; policy guest rõ; mọi route/WS được kiểm tra theo policy; test thiếu/sai/hết hạn token và reconnect. Không đánh đồng với host capability.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| UT-003 | P0 / done (M1)                             | JSON `/api/share` nhận đường dẫn tùy ý, `validatePath` không được gọi ở luồng này; peer có thể công bố file host đọc được.                                                                                                              | Chỉ local host có quyền stage đường dẫn nguồn; từ chối client trước stat/read; test absolute path, symlink, traversal, giả headers và đường dẫn ngoài allowlist nếu có.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| UT-004 | P1 / done (M2)                             | Pending chiếm active slot, hết TTL chỉ xóa server mà client không nhận terminal status; modal và task phụ thuộc WS một lần.                                                                                                             | Tách uploading/pending; batch > concurrency tiếp tục; expiry phát terminal event; reconnect query status, không kẹt khi mất complete event. Test event đến trước HTTP response.                                                                                                                                                                                                                                                                                                                                                                                                                |
| UT-005 | P1 / done (M1)                             | `createServer` merge config nhưng router/singleton dùng global; `/api/info` port/QR không phản ánh listener; static public phụ thuộc CWD.                                                                                               | Một runtime truyền cho routes/managers; port override/0, upload/temp dirs, public assets đúng từ CWD khác. Không sửa global config trong integration tests.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| UT-006 | P1 / done (M2)                             | Chunk TTL chỉ cleanup khi shutdown; orphan sau crash không có recovery; staging unshare/clear chỉ xóa Map, không xóa bản tạm.                                                                                                           | Sweep định kỳ và khi startup; cancel server-side; ownership file rõ; temp được dọn có giới hạn mà file nguồn CLI còn nguyên; test restart, TTL và unshare.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| UT-007 | P1 / done (M2)                             | Chunk manager không đối chiếu byte count thực tế; max session/devices/concurrency config chưa enforce.                                                                                                                                  | Đã triển khai memory-bounded incremental SHA-256; pre-claim concurrency slot; startup disk quota reconcile và multipart rollback; strict 64-hex checksum validation.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| UT-008 | P1 / done (M2)                             | Collision dùng exists rồi write/rename; accept xóa record trước move, lỗi đĩa mất khả năng retry. `pending-upload.js`, `chunked-upload.js`. Race cần tái hiện.                                                                          | Atomic reservation/exclusive creation; accept transactional; lỗi move giữ trạng thái retry; đồng thời cùng tên không ghi đè; test EXDEV, EACCES/ENOSPC, hai request complete/accept; R11: `SimpleUploadQuotaStorage` dùng `reserveWritableFile` tránh TOCTOU race.                                                                                                                                                                                                                                                                                                                             |
| UT-009 | P1 / done (M2)                             | pause abort chunk bị vòng retry xử lý như lỗi; cancel trong backoff có thể gửi tiếp; init/complete không abort; retry session expired không tái khởi tạo.                                                                               | Một attempt có cancellation token; pause/cancel chặn mọi retry/init/complete; resume reconcile received chunks; hết session có hành vi rõ; kiểm thử fake clock + HTTP/XHR thực tại seam.                                                                                                                                                                                                                                                                                                                                                                                                       |
| UT-010 | P1 / done (M1)                             | `wss.close()` không đóng ngay socket; server.close callback mới terminate WS, có thể treo shutdown. Batch start/stop taskkill mọi process giữ 8080.                                                                                     | Stop có deadline, close WS rồi HTTP, cleanup; quản lý đúng PID/instance của app; không kill ứng dụng khác; port occupied báo lỗi; test với WS mở.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| UT-011 | P2 / done (M3)                             | showToast nhận object nhưng nhiều caller app/drop-zone dùng hai đối số string; notification trống/sai loại.                                                                                                                             | Đã sửa 17 call site sang dạng object; `resolveToastContent` tách khỏi DOM để test được; dạng cũ vẫn hiện message kèm `console.warn` thay vì trống im lặng; rule ESLint `no-restricted-syntax` chặn tham số đầu là literal/template/binary. `describeReason` thay `event.reason?.message \|\| event.reason`.                                                                                                                                                                                                                                                                                    |
| UT-012 | P1 / done (M3)                             | Hỏi host sau khi upload hết; không có preflight consent.                                                                                                                                                                                | Đã chốt với người phụ trách: duyệt **theo lô, riêng từng file**, có **ghi nhớ thiết bị tin cậy**. Offer metadata + grant dùng một lần qua header, gate chạy trước Multer nên từ chối không ghi payload; bind theo `(tên, cỡ)` và `connectionId`; hết hạn/mất host phát terminal event; file đã consent lưu thẳng, không hỏi lại. Quyết định: `docs/adr/0003-consent-before-transfer.md`.                                                                                                                                                                                                       |
| UT-013 | P2 / done (M2)                             | Range parser chỉ start-end; suffix range và end vượt EOF bị xử lý chưa chuẩn.                                                                                                                                                           | Test bytes=-N, N-, end vượt EOF, empty file, malformed/multi-range; hỗ trợ hoặc từ chối theo hợp đồng HTTP; stream hủy khi client ngắt.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| UT-014 | P2 / done (M2)                             | Tên Unicode/reserved Windows/trailing dot/255 byte, prefix staging không được sanitize đầy đủ.                                                                                                                                          | Test Windows CON/NUL/COM1, dấu chấm cuối, tên Unicode dài theo bytes, hai tên sau sanitize trùng; lưu bằng ID nội bộ, metadata giữ tên hợp lệ.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| UT-015 | P2 / done (M1)                             | API trả path nội bộ ở upload/complete/decision; registry tin deviceId trùng, một tab đóng có thể xóa máy vẫn online.                                                                                                                    | Response không lộ đường dẫn; session identity do server quản lý; ref count nhiều tab hoặc connection ID; test spoof IDs và reconnect.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| UT-016 | P2 / done (M3)                             | Cache name thủ công, thiếu release/version update; PWA và Wake Lock trên HTTP LAN có hạn chế secure context.                                                                                                                            | Cache name lấy từ `package.json`, server chèn khi phục vụ `/sw.js`; `capabilities.js` phát hiện và **nói rõ** capability thiếu; update prompt thật qua `SKIP_WAITING` + `controllerchange` (install không còn `skipWaiting()` vô điều kiện). Sửa hai lỗi thật: listener `visibilitychange` nằm trong guard serviceWorker, và `catch {}` trống trong `_acquireWakeLock`. **Chưa** kiểm browser thật và HTTPS LAN — xem giới hạn cuối mục 4.                                                                                                                                                     |
| UT-017 | P2 / done (M2)                             | errorHandler biến JSON parse/Multer errors thành 500 và không xét headersSent.                                                                                                                                                          | Map lỗi client về 400/413, lỗi stream sau headers destroy/next thích hợp; không double-send; test malformed JSON, oversized multipart, stream failure.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| UT-018 | P2 / done (M3)                             | npm ci báo Multer 1.x deprecated dù audit 0 advisory; README khẳng định production-grade/10GB+/Wi-Fi throughput chưa có bằng chứng tải thật.                                                                                            | Đã tra upstream ngày 2026-09-16 và nâng `multer` 1.4.5-lts.2 → 2.4.0; regression multipart pass, không phải sửa call site. Bản ghi có ngày + phiên bản: `docs/dependency-review.md`. README cập nhật theo số đo thực và ghi rõ phần chưa kiểm chứng; `maxFileSize` mặc định 10 GiB là đúng. **Đính chính tiền đề**: README hiện tại không hề chứa các chuỗi "production-grade", "10GB+" hay "Wi-Fi throughput" — đã grep xác nhận, nên không có gì để gỡ; các claim còn lại (badge test/coverage thời M1, mô tả luồng duyệt cũ, "hiệu năng cao siêu mượt mà") mới là thứ đã sửa.               |
| UT-020 | P2 / done (M4)                             | Danh tính client chỉ là `connectionId` theo từng socket; reconnect tạo danh tính mới nên không thể "gửi cho một thiết bị" ổn định. `discovery.js`, `handlers.js`, `connection.js`.                                                      | Khóa danh tính do server suy ra (`conn:`/`dev:`/`sess:`), index trong discovery, `X-Connection-Id` không tạo khóa; register mang `deviceToken`; reconnect giữ được khóa bền. Test: `tests/unit/client-identity.test.js`, `tests/unit/discovery.test.js`, `tests/integration/websocket.test.js`, `tests/integration/relay-transfer.test.js` (mô tả "durable identity without a PIN").                                                                                                                                                                                                           |
| UT-021 | P1 / done (M4)                             | Chỉ host duyệt; không có khái niệm receiver ở bất kỳ record nào; `transfer:offer` và `upload:request` hard-code host-only.                                                                                                              | `RelayTransferService` + `/api/relay/*`: receiver quyết định, host **không** có nhánh bypass (`403 RELAY_FORBIDDEN`), receiver offline → `409`, offer hết hạn phát `relay:offer:expired` và ghi 0 byte, grant một lần bind `relayId` + receiver keys dùng chung cửa ghi với M3. Test: `tests/unit/relay-transfer.test.js` (ma trận grant chạy trên cả hai service), `tests/integration/relay-transfer.test.js` (3 client + host socket).                                                                                                                                                       |
| UT-022 | P1 / done (M4)                             | `GET /api/download/:fileId` không có ACL: mọi client đã xác thực tải được mọi file; `/api/shared` liệt kê tất cả.                                                                                                                       | File relay vào `tempDir/relay` (không vào `uploadDir`), TTL `relayTtlMs`, xoá + release quota khi hết hạn/revoke; ACL tải chỉ cho receiver (identity key hoặc `?rt=` một lần, hash-only), host không tải được; `/api/shared` loại file relay; sweep + reconcile gồm thư mục relay. Test: `tests/integration/relay-acl.test.js`, `tests/unit/relay-transfer.test.js`.                                                                                                                                                                                                                           |
| UT-023 | P2 / done (M4)                             | Chưa có UI chọn người nhận, chưa có hộp thư cho receiver, host không thấy relay.                                                                                                                                                        | Picker "Send to" trong Upload view; modal relay chỉ hiện trên thiết bị nhận; view "Incoming" tải file bằng capability đã lưu; card Relay trong Settings cho host (metadata + Stop, không có nút tải). Test: `tests/unit/relay-inbox.test.js`, `tests/unit/transfer-consent-engine.test.js` (nhánh relay).                                                                                                                                                                                                                                                                                      |
| UT-024 | P3 / done (M4)                             | Chưa có ADR cho mô hình quyền relay; CONTEXT.md xếp Receiver/Relay vào "tương lai".                                                                                                                                                     | `docs/adr/0004-relay-receiver-authority.md`; cập nhật CONTEXT.md, REPORT-ROADMAP.md, README.md và `docs/tasks/UT-020…024.md`. Không mô tả tính năng chưa ship; trạng thái ADR cập nhật theo vòng QC.                                                                                                                                                                                                                                                                                                                                                                                           |
| UT-025 | P2 / done (M5) — đã sửa theo QC vòng 1     | Chưa có ADR cho P2P; chưa có đặc tả signaling; chưa định nghĩa phép đo "payload không đi qua host" mà mốc M5 yêu cầu.                                                                                                                   | `docs/adr/0005-p2p-transport-authority.md` (luật chọn transport, ràng buộc chuyển tiếp, framing/backpressure/integrity, fallback, giới hạn); `docs/protocol/p2p-signaling.md` (event + mã lỗi + giới hạn); `docs/benchmarks/p2p-traffic-protocol.md` (máy/mạng/file/số lần/chỉ số, định nghĩa "không đi qua host"). Cập nhật `CONTEXT.md`. Chưa có code. **QC vòng 1:** state machine có `completed` và `connected` không terminal; phiên bind theo từng `fileIndex` đã accept; thêm trần sink `p2pMaxInMemoryBytes`; làm rõ DTLS không phải bảo đảm chống host độc hại.                       |
| UT-026 | P1 / in-progress (M5) — **GATE**           | Chưa biết `RTCPeerConnection` + host candidate có chạy trên origin HTTP LAN hay không; đây là giả định lớn nhất của M5 và không được phép đoán.                                                                                         | `public/spike/p2p.html` + `p2p.js`: trao đổi SDP thủ công, không phụ thuộc signaling của app; in `iceGatheringState`, loại candidate (host/srflx/relay, mDNS hay IP thật), `connectionState`, thời gian tới `connected`, và gửi 1 MiB qua DataChannel để xác nhận payload đi trực tiếp. **Kết luận go/no-go phải có từ thiết bị thật** (Windows Chrome ↔ Android Chrome, Windows Chrome ↔ iOS Safari) trước khi thi công UT-027/028. Nếu fail → mở UT-029.                                                                                                                                     |
| UT-027 | P1 / ready-for-agent (M5, chờ gate UT-026) | Server hiện **không thể** chuyển message giữa hai client (`handlers.js` nhánh `default` chỉ log); WS cũng chưa có `maxPayload`, rate limit hay kiểm `bufferedAmount`. M4 lại quyết định **theo từng file**, không có "accepted" cấp lô. | `src/services/p2p-signaling.js` + case `p2p:signal`/`p2p:state`; phiên bind theo tập `(relayId, fileIndex)` đã accept (tra qua `getRelay()` để đọc cả `relays` và `recent`), mọi frame mang `fileIndex`; state machine `connecting → connected → completed \| failed` với `connected` **không** terminal, `completed` chỉ từ receiver, race `failed`→`completed` thì `completed` thắng; chỉ chuyển tới đúng đối tác, không broadcast; cap size + rate + TTL; `maxPayload` cho WS và kiểm `bufferedAmount`; config `p2p*` đóng băng. Test tích hợp A/B/C + host gồm batch mixed accept/decline. |
| UT-028 | P1 / ready-for-agent (M5, chờ gate UT-026) | Transport hiện gắn cứng vào XHR/HTTP trong `_startUpload`; không có framing/backpressure/integrity cho DataChannel; chưa định nghĩa byte nhận được ghi ở đâu.                                                                           | Seam dispatch theo transport (mặc định giữ nguyên đường relay/HTTP); `public/js/p2p/framing.js` (frame có `fileIndex`), `backpressure.js`, `p2p-sink.js` (**trần bộ nhớ `p2pMaxInMemoryBytes`**, hash theo chunk, chỉ finalize sau checksum, dọn sink khi cancel/mismatch), `transport.js` (state machine + timeout) đều **unit test được không cần browser**; `supportsP2P()` trong `capabilities.js`; thêm module mới + `relay-inbox.js` vào `STATIC_ASSETS` và bump cache version. Chưa bật data plane.                                                                                     |
| UT-029 | P3 / needs-info (M5, mở có điều kiện)      | Nếu UT-026 kết luận HTTP LAN không dùng được P2P thì phải có TLS LAN trước (secure context, cookie `Secure`, Wake Lock, PWA install).                                                                                                   | Chứng chỉ self-signed/local CA + cách phân phối trust; ảnh hưởng tới QR/launcher; kiểm lại cookie `Secure` và các capability phụ thuộc secure context. Chỉ mở khi gate yêu cầu.                                                                                                                                                                                                                                                                                                                                                                                                                |

Năm mục `done (M1)` ở trên (UT-002, UT-003, UT-005, UT-010, UT-015) đã hoàn tất nghiệm thu và chính thức merge vào `main` (commit `64cff86`). CI GitHub Actions đã chạy pass trên ma trận Ubuntu/Windows và Node 22/Node 24.
Các mục M2 (UT-004, UT-006, UT-007, UT-008, UT-009, UT-013, UT-014, UT-017) đã giải quyết toàn bộ 13 điểm yêu cầu từ QC review (R1–R4 vòng 1, R5–R10 vòng 2, R11–R12 vòng 3 và R13 xác minh host upload trong `REVIEW-M2.md`).

### Bằng chứng lượt M3 (2026-09-16, nhánh `m3-core-ux-consent`, đã merge qua PR #3)

`npm run quality` pass trên Node 24.15.0 / Windows: **424 automated tests pass, 0 fail**, coverage **90,13% line / 83,01% branch / 87,66% function** (baseline trước M3: 88,12 / 80,02 / 84,79 — cả ba chỉ số đều tăng), lint và format sạch.

UT-012 có negative test chạy qua HTTP/WS thật: upload không grant bị chặn ngay ở gate **trước khi Multer parse** (428) và không byte nào được ghi; grant lệch tên hoặc lệch cỡ bị từ chối; grant dùng lần thứ hai bị từ chối; grant của connection khác bị từ chối; offer hết hạn phát `transfer:offer:expired` và để lại 0 byte; file bị từ chối không tới được cả `temp/pending`; thiết bị tin cậy được duyệt không cần hỏi host.

Hai lỗi trong chính bản vá này được test bắt và đã sửa: (1) nhánh thiết bị tin cậy phát grant nhưng không ghi `decision`, khiến offer tự duyệt nằm mãi ở trạng thái `pending` và host vẫn thấy như đang chờ; (2) khi ghi store thất bại, bản ghi trusted device vẫn nằm trong RAM dù chưa được persist, làm state trong bộ nhớ và trên đĩa lệch nhau.

**Giới hạn của lượt này — không được đọc là đã pass:** chưa chạy CI cho nhánh này; chưa test trên điện thoại thật (Android Chrome / iOS Safari); chưa có TLS LAN; vòng đời cache service worker cũ→mới chưa kiểm trên browser thật; không có E2E browser trong repo. Kiểm chứng browser thủ công bằng headless Chrome như M1 **chưa chạy** trong lượt này.

### Bằng chứng lượt M4 (2026-09-17, nhánh `m4`, đã merge qua PR #4)

`npm run quality` pass trên Node 22.23.1 / Linux: **510 automated tests pass, 0 fail**, coverage **95,43% line / 86,84% branch / 93,99% function**, lint và format sạch. Baseline trước M4 (main `d81f437`): 438 tests, 95,50 / 87,66 / 93,91.

Test M4 đi qua HTTP/WS thật (không mock service): `tests/integration/relay-transfer.test.js` dựng **3 client + 1 host socket** và khẳng định `relay:offer` chỉ tới receiver (sender, observer và host đều không nhận); observer và **host** gọi `/api/relay/decision` đều `403 RELAY_FORBIDDEN`; receiver offline `409 RECEIVER_OFFLINE`; offer không ai trả lời phát `relay:offer:expired` cho cả hai phía và để lại **0 byte**; reconnect bằng device token vẫn duyệt được. `tests/integration/relay-acl.test.js` khẳng định file relay nằm ở `tempDir/relay` chứ không vào `uploadDir`, không xuất hiện trong `/api/shared`, receiver tải được bằng `?rt=` **và** bằng device token, range 206 hoạt động, còn observer/nút host/sai token đều `403`; hết `relayTtlMs` thì file bị xoá và quota được trả lại; host revoke xoá được file mà vẫn không tải được nó. `tests/unit/relay-transfer.test.js` chạy **cùng một ma trận từ chối grant trên cả `TransferOfferService` (M3) và `RelayTransferService` (M4)** để hai implementation không lệch nhau.

**Giới hạn của lượt M4 — không được đọc là đã pass:** chưa qua QC review; chưa chạy CI GitHub Actions cho nhánh này; **chưa kiểm trên browser/thiết bị thật** (UI chọn người nhận, modal relay, view Incoming mới chỉ có unit test với DOM giả); chưa test với file lớn; chưa có TLS LAN.

### Phản hồi QC M4 vòng 1 — commit `e6d4560` (2026-09-17)

QC kết luận **chưa đủ điều kiện merge** với 5 phát hiện (`REVIEW-M4-QC.md`). Đã khắc phục toàn bộ trên cùng nhánh `m4`:

- **M4-QC-01 (P0) — capability tải bị broadcast cho sender.** `POST /api/relay/decision` nay tách hai DTO: response HTTP cho receiver chứa `relayToken` đúng một lần, còn event `relay:decision` tới sender chỉ còn `{ index, decision, grantId }`. Regression: `tests/integration/relay-transfer.test.js` quét **mọi** event của sender/observer/host/receiver sau Accept và khẳng định không có chuỗi raw token lẫn field `relayToken`, đồng thời khẳng định sender vẫn nhận được `grantId` để upload.
- **M4-QC-02 (P1) — readback tin `X-Connection-Id` tự khai.** `GET /api/relay/offer/:relayId` nay chỉ nhận danh tính qua identity key đã bind, hoặc qua `resolveSender(req, { required: true })` (đối chiếu socket sống + IP + session). Regression: C mang session của mình nhưng khai connection ID của A nhận `403`; A và B hợp lệ vẫn đọc được `200`.
- **M4-QC-03 (P1) — chunked relay mất reservation sau complete.** `ChunkedUploadManager.complete()` nhận option `releaseQuota`; đường relay truyền `releaseQuota: false` để reservation chuyển sang file đã lưu, và relay service là chủ sở hữu duy nhất release khi hết TTL/revoke. Regression: baseline quota → complete → `baseline + size` → revoke → đúng `baseline`; thêm case ép `shareManager.addFile` lỗi sau bước merge để chứng minh không leak file/quota và không double-release.
- **M4-QC-04 (P1) — `relay:stored`/`relay:downloaded` không được nối vào luồng thật.** `attachStoredFile()` nay phát `relay:stored`; route download gọi `markDownloaded(fileId)` **chỉ** khi response `200` toàn bộ kết thúc thành công. Regression: `relay:stored` cho cả simple và chunked; tải full phát đúng **một** `relay:downloaded`; range `206`, tải lại lần hai và client abort giữa đường đều **không** phát.
- **M4-QC-05 (P2) — UI/ADR tuyên bố quá mức về khả năng đọc của host.** Đã đổi wording ở `public/js/relay-inbox.js`, `public/js/app.js`, `README.md`, `CONTEXT.md`, `docs/adr/0004` và `docs/tasks/UT-022.md` sang "app không cho host tải" + ghi rõ **đây là ranh giới quyền trong app, không phải mã hoá đầu cuối** (file vẫn plaintext trong `tempDir/relay`).

Bằng chứng sau khi vá: `npm run quality` pass trên Node 22.23.1 / Linux — **515 automated tests pass, 0 fail**, coverage **95,48% line / 87,04% branch / 94,04% function**, lint và format sạch. Suite `tests/integration/relay-acl.test.js` được chạy lại 3 lần liên tiếp để kiểm tra độ ổn định của case abort (12/12 pass mỗi lần). **Chưa** chạy lại CI GitHub Actions cho commit khắc phục, và **chưa** smoke test browser/thiết bị thật.

### Phản hồi QC M5 vòng 1 — commit `24311ab` (2026-09-18)

QC kết luận **chưa đủ điều kiện merge M5**: UT-026 vẫn là gate chưa chạy, và thiết kế còn ba blocker P1 + một P2. QC cũng tự vá bốn điểm nhỏ (spike bypass SW cache, throughput đo ở receiver, spike vào lint/format gate, test PWA cho hành vi network-only). Đã sửa toàn bộ phần **thiết kế** trên cùng nhánh `m5`:

- **M5-QC-01 (P1) — `connected` bị coi là terminal nên không thể fallback sau lỗi truyền.** State machine nay là `connecting → connected → completed | failed`; `connected` **không** terminal; `completed` chỉ receiver được báo và chỉ sau khi đủ byte + checksum; `failed` được chấp nhận trước `completed`; báo lại cùng trạng thái là no-op; race `failed` rồi `completed` thì `completed` thắng (`recoveredAfterFailure: true`) và sender phải huỷ/thu hồi phần relay của mình. Ghi ở `docs/protocol/p2p-signaling.md` §2.1 + ADR-0005 quyết định 11; test `connected → checksum mismatch → failed → relay` nằm trong acceptance của UT-027.
- **M5-QC-02 (P1) — signaling bind theo `relayId` trong khi consent của M4 là theo từng file.** Phiên nay bind theo tập `(relayId, fileIndex)` đã accept; mọi frame mang `fileIndex`; receiver chỉ mở sink cho index đã accept; tra relay qua `getRelay()` để đọc cả `relays` và `recent`; phiên giữ bản sao `acceptedIndexes`/party keys. Ghi ở §1 và §5, kèm mã lỗi `P2P_NOT_ACCEPTED`; test batch mixed accept/decline nằm trong acceptance của UT-027.
- **M5-QC-03 (P1) — chưa có chiến lược sink bounded-memory.** Trên HTTP LAN không có File System Access nên sink là bộ nhớ: thêm trần công khai `p2pMaxInMemoryBytes` (mặc định 64 MiB), file vượt trần **không thử P2P** mà đi relay ngay từ đầu; hash cập nhật theo từng chunk; chỉ finalize sau khi checksum khớp; cancel/mismatch phải dọn sink. Ma trận đo đổi ca 1 GiB thành ca âm và thêm ca 64 MiB đo peak buffer. Ghi ở ADR-0005 quyết định 9 và acceptance của UT-028.
- **M5-QC-04 (P2) — tuyên bố quá mức về DTLS/signaling.** ADR-0005 quyết định 2 và 14 + `CONTEXT.md` nay nói rõ: "host không nhận payload" là **bất biến của implementation**, không phải bảo đảm mật mã; host nằm giữa đường signaling (phát SDP kèm fingerprint DTLS) và dự án **chưa có** xác thực peer độc lập, nên host bị chiếm quyền vẫn có thể xen vào giữa.

**Còn lại để qua gate:** chạy UT-026 trên Windows Chrome ↔ Android Chrome và Windows Chrome ↔ iOS Safari, ghi `docs/benchmarks/p2p-spike-<ngày>.md`. UT-027/028 vẫn `ready-for-agent` và **chưa được thi công**.

Các vấn đề security phải được sửa trước khi mở rộng phạm vi người dùng LAN. Không giải quyết auth bằng việc ẩn nút ở frontend. Host-only approval không ngăn đường upload/stage khác chiếm ổ đĩa; quota/route policy vẫn cần làm.

## 5. Core feature roadmap

| Mốc                       | Nội dung                                                                                                                                                                                                                                              | Dependency / điều kiện ra khỏi mốc                                                                                                                                                                                                                                                                                                        |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M0 — Host approval        | UT-001, regression HTTP/WS, bootstrap host, tài liệu và gates.                                                                                                                                                                                        | Hoàn thành: Đã merge `cd5d5be`.                                                                                                                                                                                                                                                                                                           |
| M1 — Quyền và runtime     | UT-002, UT-003, UT-005, UT-010, UT-015 — đã hoàn thành và merge vào `main` (commit `64cff86`).                                                                                                                                                        | Hoàn thành: Negative auth tests pass; host/client role thực; QR/runtime đúng; lifecycle shutdown deadline an toàn; CI Ubuntu/Windows pass; giải quyết triệt để R1–R7.                                                                                                                                                                     |
| M2 — Truyền file tin cậy  | UT-004, UT-006–009, UT-013–014, UT-017 — hoàn thành giải quyết QC review R1–R13 trên nhánh `m2-reliable-transfer`.                                                                                                                                    | Hoàn thành R1–R13: atomic reservation chống ghi đè, resource/integrity/retry controls, terminal routing, session-bound connection identity và host upload với capability đã xác minh.                                                                                                                                                     |
| M3 — Core UX và consent   | UT-011, UT-012, UT-016, UT-018 — đã hoàn thành và merge vào `main` qua PR #3 (nhánh `m3-core-ux-consent`).                                                                                                                                            | Một phần: host duyệt trước truyền **đạt**; thông báo mọi terminal state **đạt** (offer hết hạn, từ chối, lỗi đều có sự kiện và câu chữ); capabilities browser **chưa** kiểm chứng trên thiết bị thật — chỉ có feature detection và thông báo trong UI.                                                                                    |
| M4 — Receiver giữa client | UT-020–UT-024 — đã hoàn thành, qua hai vòng QC (`REVIEW-M4-QC.md`) và merge vào `main` qua PR #4. Quyết định: `docs/adr/0004-relay-receiver-authority.md`.                                                                                            | Một phần: chọn receiver, consent thuộc receiver (host không duyệt/không có đường tải), ACL theo file, TTL/quota **đạt** ở tầng HTTP/WS, kể cả sau khi vá M4-QC-01…05 và hai blocker vòng 2. **Chưa** kiểm browser/thiết bị thật. Chốt sản phẩm cho M5 vẫn còn: auto-save sau consent, trust ghi nhớ cho relay, TLS LAN.                   |
| M5 — P2P thử nghiệm       | WebRTC DataChannel và signaling host; file framing, ordered chunk IDs, bufferedAmount backpressure, progress/checksum, cancel. **Trên nhánh `m5`: đã có thiết kế (UT-025, đã sửa theo QC vòng 1) và trang spike (UT-026), data plane CHƯA thi công.** | M1–M3 ổn định; đo traffic chứng minh payload A↔B không đi qua host; ICE failure được báo, relay fallback chỉ theo policy rõ. **Gate chưa xong:** spike UT-026 phải chạy trên thiết bị thật (Windows Chrome ↔ Android Chrome, Windows Chrome ↔ iOS Safari) trước khi làm UT-027/028; nếu ICE fail trên HTTP LAN thì mở UT-029 (TLS) trước. |
| M6 — Desktop beta         | Host app Windows/Linux: tray, start/stop, QR public, approval, settings, file picker, notification, single-instance.                                                                                                                                  | M1–M3 là bắt buộc; P2P có thể ship sau desktop, không làm chặn bản host beta.                                                                                                                                                                                                                                                             |
| M7 — Release              | Installer, sign/checksum/SBOM, update/rollback, clean-machine tests, support docs.                                                                                                                                                                    | Quality + browser/device + packaging matrix pass; không còn P0/P1 mất dữ liệu.                                                                                                                                                                                                                                                            |

M4/M5 là đề xuất mở rộng, chưa được coi là yêu cầu triển khai P2P ngay. Chốt thêm: client B cần tải thủ công hay auto-save sau consent; file history metadata hay lưu payload; trust theo từng phiên hay ghi nhớ thiết bị; TLS LAN provisioning.

## 6. Tối ưu cần đo

- Thu thập baseline trước tối ưu: 1/3/5 transfer đồng thời; 10 MiB/100 MiB/1 GiB và gần giới hạn; peak RSS, temp disk, CPU, throughput, latency event loop, thời gian merge.
- Với 10 GiB, lúc merge có thể giữ cả chunks và file ghép gần 20 GiB: reserve disk trước nhận, đừng chỉ nhìn fileSize; EXDEV còn có thể thêm bản copy.
- Chunk memoryStorage giữ buffer theo số request đồng thời; áp backpressure/concurrency/quota trước tăng chunk size.
- Preview dùng full image download: tạo thumbnail có giới hạn dimensions/bytes và cache LRU; kiểm tra định dạng unsupported (HEIC) thay vì hứa preview mọi ảnh.
- Hiện drop-zone gửi một multipart lớn, không đi qua TransferEngine progress/cancel/retry: thống nhất luồng upload có mục đích share/receive sau khi state machine ổn định.
- Dùng cập nhật metadata dạng delta và DOM patch/windowed list khi benchmark với hàng nghìn file chứng minh full re-render gây chậm. Chưa cần framework mới chỉ để tối ưu.
- Duyệt folder phải giữ relative path nếu sản phẩm cam kết gửi nguyên thư mục; hiện flatten basename. Thêm manifest an toàn, directory picker và download batch/archive theo nhu cầu, kiểm tra traversal.

Không đặt mục tiêu RAM/MB/s tùy ý làm quality gate trước khi có baseline trên thiết bị thật. Định nghĩa máy, mạng, file, số lần đo và p50/p95 trong report benchmark.

## 7. Đóng gói Windows và Linux

### Hướng đề xuất

Electron tái sử dụng UI JS và Node backend hiện có. Đây là đề xuất để spike/ADR, chưa thêm Electron dependency hoặc sinh installer trong lượt này. Phân tách lifecycle server trước: start trả runtime, stop await được, không process.exit từ module, không gắn signal handler lặp; launcher/desktop main sở hữu lifecycle.

Tauri + Node sidecar có thể được đánh giá nếu kích thước là ưu tiên, nhưng cần Rust/toolchain và quản lý sidecar. Node standalone binary phù hợp headless/tray riêng; tự đóng gói Node không tự tạo desktop approval UX. Đo kích thước/cold-start/RSS của spike rồi chốt ADR.

### Các task đóng gói

| ID      | Đầu ra                   | Tiêu chí nghiệm thu                                                                                                                                                         |
| ------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PKG-001 | Desktop shell spike      | Host mở/tray, start/stop server, QR guest, approval; close-window vs quit rõ; app single-instance; chạy từ CWD bất kỳ.                                                      |
| PKG-002 | Security desktop         | Renderer sandbox, contextIsolation, nodeIntegration off; preload IPC allowlist/schema; không load nội dung remote có Node quyền; chặn navigation/window.open bất ngờ.       |
| PKG-003 | Paths/settings           | Resources readonly theo app path; config/log/temp theo userData hoặc OS data directory; nhận file theo thư mục user chọn; migration versioned và reset không xóa file nhận. |
| PKG-004 | Windows build            | NSIS installer `.exe`, tùy chọn portable `.exe`; icons/app ID/version/license; standard user, firewall hướng dẫn rõ, Unicode path; không cần Node cài ngoài.                |
| PKG-005 | Linux build              | AppImage cho portable; deb cho Ubuntu/Debian sau beta; desktop entry/icon, tray fallback khi desktop environment thiếu hỗ trợ; binary Linux tương ứng kiến trúc.            |
| PKG-006 | Release pipeline         | Build Windows trên Windows, Linux trên Linux; lock dependencies, artifacts SHA-256, SBOM/license, release notes, retention, không chứa secret hay fixture.                  |
| PKG-007 | Update/rollback          | Thử upgrade N→N+1 và rollback không mất data/settings; download update lỗi không làm hỏng app; Windows ký số trước phát hành rộng; private keys chỉ CI secrets.             |
| PKG-008 | Clean-machine validation | VM không Node: install/start/send/accept/decline/restart/update/uninstall; dữ liệu nhận còn sau uninstall theo UX; test port occupied và LAN không Internet.                |

Không build một `.exe` rồi coi là bản Linux. Mục tiêu artifact ban đầu đề xuất Windows x64 + Linux x64; ARM64 là backlog sau khi có nhu cầu và runner/test thiết bị.

Nguồn kiểm tra ngày 2026-09-14: [Electron distribution](https://www.electronjs.org/docs/latest/tutorial/distribution-overview), [Electron sandbox](https://www.electronjs.org/docs/latest/tutorial/sandbox), [electron-builder targets](https://www.electron.build/docs/targets/). Tài liệu upstream cần được kiểm tra lại khi pin version để build; không ghi version mới nhất cố định vào roadmap.

## 8. Hệ thống chất lượng và phân công

Entry point agent: `AGENTS.md`. Domain: `CONTEXT.md`. Authority: `docs/adr/0001-host-approval-authority.md`. Quy chuẩn: `docs/agents/quality.md`. Backlog/status: `docs/agents/issue-tracker.md`, `docs/agents/triage-labels.md`.

`npm run quality` là gate thực thi lint + format + all tests/coverage; CI ma trận Windows/Ubuntu, Node 22/24. PR template yêu cầu ID, acceptance, kết quả thực tế, giới hạn và rollback. Chưa bật branch protection online và chưa tạo issue GitHub.

Phân việc tương lai: agent A auth/runtime (M1), agent B lifecycle/disk sau interface M1 ổn định, agent C browser tests/core UX, agent D packaging spike sau stop/runtime API ổn định. Không cho nhiều agent cùng sửa transfer.js/server.js mà chưa thống nhất ownership. Agent chính tích hợp, chạy gates cuối và cập nhật status.

Tiêu chuẩn hoàn tất: acceptance đạt và test trên đúng seam; không chỉ “build thành công”. Cần ghi riêng automated local, CI, browser E2E, physical device và clean-machine packaging. Không tự đánh dấu các mức chưa thực hiện là pass.
